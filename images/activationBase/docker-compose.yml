services:
  activation_base:
    build: .
    image: energy_predictor/activationbase:latest
    volumes:
      - ai_system:/tmp
    command:
      - sh
      - "-c"
      - |
        rm -rf /tmp/activationBase/ && cp -r ../activationBase/ /tmp/;
volumes:
  ai_system:
    external: true